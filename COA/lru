#include <stdio.h>

int main() 
{
    
    int cache,n,flag,arr1[100],input;
    int hit=0;
    
    printf("Enter cache size: ");
    scanf("%d",&cache);
    printf("Enter number of inputs: ");
    scanf("%d",&n);
    
    for(int i=0;i<n;i++)
    {
        printf("Enter input %d: ",i+1);
        scanf("%d",&input);
        flag=0;
        for(int j=0;j<i;j++)
        {
            if(arr1[j]==input)
            {
                hit = hit + 1;
                flag=1;
                break;
            }
        }
        if(flag==0)
        {
            arr1[i]=input;
        }
        
    }
    for(int i=0;i<n;i++)
    {
        printf("%d\n",arr1[i]);
    }
    printf("%d",hit);
    return 0;
}
